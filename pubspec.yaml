name: resume
description: A new Flutter project for Resume

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

scripts:
  go: flutter run -d chrome

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  liquid_container: ^1.0.4
  flutter_tilt: ^1.0.4
  animated_glitch: ^2.0.0+2
  gif_view: ^0.4.0
  scroll_loop_auto_scroll: ^0.0.5
  lecle_bubble_timeline: ^0.0.1+1
  card_actions: ^0.0.1
  animated_text_kit: ^4.2.2
  blurrycontainer: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
